{
  "$schema": "../../../node_modules/nx/schemas/project-schema.json",
  "name": "models",
  "projectType": "library",
  "sourceRoot": "libs/angular/models",
  "targets": {
    "build": {
      "executor": "nx:run-commands",
      "options": {
        "command": "echo 'Placeholder build target for angular models library'"
      }
    }
  }
}
