{
  "$schema": "../../../node_modules/nx/schemas/project-schema.json",
  "name": "auth",
  "projectType": "library",
  "sourceRoot": "libs/angular/auth",
  "targets": {
    "build": {
      "executor": "nx:run-commands",
      "options": {
        "command": "echo 'Placeholder build target for angular auth library'"
      }
    }
  }
}
