{
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "name": "backend",
  "projectType": "application",
  "sourceRoot": "apps/backend",
  "targets": {
    "build": {
      "executor": "nx:run-commands",
      "options": {
        "command": "echo 'Placeholder build target for backend'"
      }
    },
    "serve": {
      "executor": "nx:run-commands",
      "options": {
        "command": "echo 'Placeholder serve target for backend'"
      }
    }
  }
}
