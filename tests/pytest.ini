[pytest]
log_level=DEBUG
log_format='%(asctime)s %(levelname)s %(name)s :: %(message)s'
log_date_format= %Y-%m-%d %H:%M:%S

markers = 
    thisOne : test tag to run single tests while writting them
    functional : functional tests
    non_functional: non functional tests